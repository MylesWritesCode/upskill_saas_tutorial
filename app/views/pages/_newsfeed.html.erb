<div class="row">
  <div class="newsfeed">
    <div class="col-md-4 col-md-offset-4 text-center btn btn-success btn-lg">
      <div class="profile-link-on-homepage">
        <%= link_to "Create your profile!", new_user_profile_path(user_id: current_user.id) if user_signed_in? %>
      </div>
    </div>
  </div>
</div>